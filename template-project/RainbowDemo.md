# 🌈 Rainbow Magic Effect Demo

This note showcases the reactive python scripting integration.

When you switch this note to **Preview Mode** (using `meta+e` or the button in the header), the background Python runner executes `rainbow.py`. It receives the exact file path and line number, applies rainbow coloring to each character of the paragraph below, and then deletes the trigger expression from this note.
